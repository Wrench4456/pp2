#calculo de resistencia

#entrada de arquivos 
r1 = float(input("Quantos volts tem a primeira resistencia?"))
r2 = float(input("Quantos volts tem a segunda resistencia?"))
r3 = float(input("Quantos volts tem a terceira resistencia?"))
r = r1 + r2 + r3

#saida de arquivos
print(f"O valor total que as resistencias equivalem é {r}v")
